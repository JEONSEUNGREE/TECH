// let, const, var
/*
1. TypeScript는 안전하고 예측 가능한 코드 구현에 초점을 맞춘 JS의 상위 집합 언어
2. TypeScript의 가장 큰 특징은 JS에 Type System을 적용한것
3. TypeScript로 작성된 코드는 TypeScript 컴파일러를 통해 JS로 변환되고 이후 실행된다.
4. Type System 이외에도 TypeScript는 Decorators와 같은 다양한 기능을 제공한다.
5. 인터프린터 형태의 Dynamic Type
6. 다른 타입을 전달하면 컴파일시점에서 에러발생
7. 기존의 타입추론형태의 자바스크립트를 타입스크립의 타입 유추 변수로 확정한다.
*/

// install : sudo npm install -g typescript ts-node



