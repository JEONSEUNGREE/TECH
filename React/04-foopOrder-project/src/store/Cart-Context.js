import React from 'react';

React.createContext({
    item: [],
    totalAmount: 0,
    addItem: (item) => {},
    removeItem: (id) => {},
});

const CartContext = () => {
    return (
        <div>
            
        </div>
    );
}

export default CartContext;
