ch7-5,6 단일상속, Object클래스

Java는 단일 상속만 허용 
다중상속 효과 내기
1. 클래스 여러개 필요한 경우 둘다 포함관계로 두던지
2. 아니면 비중이 높은 클래스를 상속받고 적은 클래스를 포함관계로 둔다.

Object클래스는 모든 클래스의 조상
부모가 없는 클래스는 자동적으로 Object 클래스를 상속받게된다.
EX) class Tv === class Tv extends Object (컴파일러 자동추가)


