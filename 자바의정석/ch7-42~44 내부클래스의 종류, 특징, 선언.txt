ch7-42~44 내부클래스의 종류, 특징, 선언

클래스 안의 클래스 (inner class)

내부 클래스의 장점 (멤버라고 보면됨)
-내부 클래스에서 외부 클래스의 멤버들을 쉽게 접근할 수 있다.
-코드의 복잡성을 줄일 수 있다.(캡슐화)
-객체 생성없이도 접근 가능
-메서드 내부에도 클래스 생성가능
-static 클래스도 cv처럼 사용가능

사실상 iv cv lv 같은 변수와 같이 내부클래스도 같은 맥락으로보면된다.