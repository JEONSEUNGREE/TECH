생성자 주입
필드주입
setter 주입

의존관계가 동적으로 변하는 관계가 없으므로 생성자 주입이 좋다.

*****
실무에서는 주로 정형화된 컨트롤러, 서비스, 리포지토리 같은 코드는 컴포넌트 스캔을 사용한다. 그리고 정형화 되지 않거나,
상황에 따라 구현 클래스로 변경해야 하면 설정을 통해 스프링 빈으로 등록한다.

@Autowired를 통한 DI(Dependency Injection)는 스프링이 관리하는 객체에서만 동작한다.
즉 스프링 빈으로 등록하지않고 내가 직접 생성한 객체에서는 동작하지 않는다.